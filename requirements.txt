# Daily Video Reporter — Python dependencies (Python 3.10+)
# Story 1.1 — Environment setup.

# --- TTS (Story 1.3) ---
# edge-tts: free, no API key, neural Vietnamese voices (default engine)
edge-tts>=7.0

# OmniVoice (optional, requires GPU with ≥18GB memory):
# pip install torch>=2.8 torchaudio>=2.8 omnivoice>=0.1.5 soundfile>=0.12

# --- DevZone REST client (Story 1.5) ---
requests>=2.31

# --- Dev / tests ---
pytest>=8.0

# NOTE: HeyGen HyperFrames (Story 1.4) is a Node CLI, run via `npx hyperframes`.
#       It requires Node.js 22+ and FFmpeg on PATH (installed in setup.sh).
